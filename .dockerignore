.git
.github
.dockerignore
.gitattributes
.gitignore
.idea
.vscode

__pycache__/
*.py[cod]
*$py.class
*.so
htmlcov/
.coverage
.coverage.*
.pylintrc
.pytest_cache/
.venv
venv

.DS_Store
.AppleDouble
.LSOverride
._*

*/dist
*/data/db
*/marvin/test
*/marvin/.temp

# Dev-only data. src/dev/data/seeds holds demo workspace bundles (workspace-*.json/zip)
# that init_db imports on startup — they must never ship in a production image.
src/dev
src/tests
tests

# Not used at runtime. NB: the healthcheck is marvin.scripts.healthcheck (src/marvin/scripts),
# which is unaffected by excluding the repo-root scripts/ directory.
scripts
docs
tasks
marvin-chart

# The image serves no frontend; these are build-time only.
frontend
node_modules
